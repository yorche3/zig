# Core — Zig

Implementaciones de los módulos **Core** del roadmap en **Zig**.

---

## 📂 Módulos / Modules

| Módulo | Descripción |
| ------ | ----------- |
| [`foundations/`](foundations/) | **Fase 0 — Fundamentos**: `helloworld`, `hellouser`, `unit_test/calculator`, `numbers` |

---

## ▶️ Siguiente / Next

👉 Explora los módulos disponibles en [`foundations/`](foundations/).

👉 Explore the available modules in [`foundations/`](foundations/).

---

## 🧪 Convenciones / Conventions

**ES:** Los scripts independientes se ejecutan con `zig run` o se compilan con `zig build-exe`. Los proyectos tipo librería separan `src/` y `test/`, usan `build.zig` y prueban con `std.testing` mediante `zig build test --summary all`.

**EN:** Standalone scripts run with `zig run` or compile with `zig build-exe`. Library-style projects separate `src/` and `test/`, use `build.zig`, and test with `std.testing` through `zig build test --summary all`.

---

*[← Volver a Zig](../README.md)*

*🌐 [github.com/yorche3/programming_languages](https://github.com/yorche3/programming_languages) · [GitHub Pages](https://github.com/yorche3/programming_languages)*
